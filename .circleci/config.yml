version: 2
jobs:
   build:
     docker:
       - image: iancolwell/xenial_user:latest
         environment:
           CONTINUOUS_INTEGRATION: TRUE
     steps:
       - checkout
       - run: bash scripts/install_apps.bash
